export enum User {
  LOGIN = '/login',
  USER = '/user',
  USER_OWN = '/user/own',
  USEMENU = '/use/menu'
}

export enum ContentApis {
  DASHBOARD = '/dashboard',
  TABLE = '/table',
  USER = '/user' // 用户列表
}
